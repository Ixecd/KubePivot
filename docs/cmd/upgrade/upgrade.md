# kp upgrade

跨版本全链路升级。

## 用法

```
kp upgrade [flags]
```

## Flag

| Flag | 默认值 | 说明 |
|---|---|---|
| `--target` | | 目标版本 |
| `--service` | | 指定服务 |
| `--dry-run` | `false` | 预览不执行 |
| `--force` | `false` | 跳过确认 |

## 相关命令

- `kp deploy` — 部署
- `kp release` — 发布新版本
