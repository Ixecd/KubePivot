apiVersion: v1
kind: Service
metadata:
  name: whoami-blue
  namespace: {{ .Values.namespace }}
  labels:
    app: whoami
    version: blue
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: whoami
    version: blue
