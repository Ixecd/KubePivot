apiVersion: v1
kind: Service
metadata:
  name: whoami-green
  namespace: {{ .Values.namespace }}
  labels:
    app: whoami
    version: green
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: whoami
    version: green
